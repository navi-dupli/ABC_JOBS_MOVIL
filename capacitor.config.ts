import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'ABC_JOBS_MOVIL',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
